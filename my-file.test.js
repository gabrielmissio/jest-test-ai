require('./customMatchers')
const myInput = require('./demo-input.json')
// Ensure you have create a file named demo-input.json in the same directory as this file

it('Lorem Ipsum', async () => {
    const response = {
        body: myInput
    }

    // paste the GTP output here
})